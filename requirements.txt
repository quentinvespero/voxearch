# Audio download (YouTube, SoundCloud, Acast, podcast RSS feeds, …)
yt-dlp

# Transcription — Apple Silicon optimised via Apple MLX framework
mlx-whisper

# Vector database client
qdrant-client

# HTTP server for the GUI backend
fastapi
uvicorn[standard]

# Embedding model (multilingual MiniLM, ~420 MB first-time download)
sentence-transformers

# ── System requirement (not pip-installable) ─────────────────────────────────
# ffmpeg must be installed for yt-dlp to convert audio to MP3:
#   brew install ffmpeg
